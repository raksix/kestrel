{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions granted to the main window, selection overlays and the picker",
  "windows": [
    "main",
    "overlay-*",
    "picker",
    "editor"
  ],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-start-dragging",
    "opener:default",
    "clipboard-manager:allow-write-text",
    "clipboard-manager:allow-read-text",
    "dialog:allow-open",
    "dialog:allow-save",
    "notification:default",
    "global-shortcut:default",
    "opener:allow-open-url"
  ]
}